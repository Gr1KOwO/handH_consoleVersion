public class Armor extends Item
{
    public Armor(String name, int attackModifier, int minDamage,int maxDamage,int defenseModifier, int healthModifier, int luckModifier, int evasionModifier) {
        super(name, attackModifier,minDamage, maxDamage, defenseModifier, healthModifier, luckModifier, evasionModifier);
    }
}
